# Agent docs

Documentation for each AI agent (ProductAgent, ListingAgent, PricingAgent,
CatalogAgent, CompetitionAgent, AnalyticsAgent, PromotionAgent,
ResearchAgent) and the `CommerceOrchestrator` that sequences them, added as
each agent is implemented starting Phase 9.
