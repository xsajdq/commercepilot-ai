# API docs

FastAPI serves interactive OpenAPI docs at `/docs` in every environment
during development. Hand-written API notes (auth flow, error format,
pagination conventions) go here once the API has endpoints beyond
`/health`.
