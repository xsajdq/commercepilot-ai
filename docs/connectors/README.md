# Connector docs

Per-platform notes (auth flow, rate limits, quirks) for each
`CommerceConnector` implementation, added as each connector is built:
WooCommerce (Phase 4), Allegro (Phase 5), Shoper (Phase 17), PrestaShop
(Phase 18), IdoSell (Phase 19 — needs its own research pass rather than
forcing the WooCommerce-shaped abstraction).
