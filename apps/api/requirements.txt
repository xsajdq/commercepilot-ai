fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
email-validator==2.2.0
pydantic-settings==2.7.1
sqlalchemy==2.0.36
# SQLAlchemy's own dependency spec only pulls greenlet in for
# python_version < 3.13 - on 3.13+ (this project's pinned version) it's
# silently skipped, which breaks every AsyncSession call at runtime with
# "ValueError: the greenlet library is required". Pin it explicitly.
greenlet==3.5.5
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
httpx==0.28.1
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
cryptography==44.0.0
sentry-sdk[fastapi]==2.19.2

# Local packages (see packages/shared, packages/domain) - install order
# matters: cp_domain depends on cp_shared.
-e ../../packages/shared
-e ../../packages/domain
