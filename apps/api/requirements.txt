fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
email-validator==2.2.0
pydantic-settings==2.7.1
sqlalchemy==2.0.36
# SQLAlchemy's own dependency spec only pulls greenlet in for
# python_version < 3.13 - on 3.13+ (this project's pinned version) it's
# silently skipped, which breaks every AsyncSession call at runtime with
# "ValueError: the greenlet library is required". Pin it explicitly.
greenlet==3.5.5
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
httpx==0.28.1
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
cryptography==44.0.0
sentry-sdk[fastapi]==2.19.2
# apps/api only ever enqueues tasks by name (see app/core/celery_client.py)
# - it never imports apps/worker's task modules, so no `include=[...]`
# worker config is needed here, just the client library.
celery[redis]==5.4.0
stripe==11.4.1

# Local packages (see packages/shared, packages/domain, packages/pricing,
# packages/analytics, packages/ai, packages/policies, packages/billing) -
# install order matters: each depends on the ones above it. apps/api needs
# the AI/approval packages for real now (the recommendations API resolves
# and runs approved tool calls), not just for its test suite; it also
# computes the live analytics dashboard directly via cp_analytics, and
# checks/reports real AI spend via cp_billing (Phase 20).
-e ../../packages/shared
-e ../../packages/domain
-e ../../packages/pricing
-e ../../packages/analytics
-e ../../packages/ai
-e ../../packages/policies
-e ../../packages/billing
