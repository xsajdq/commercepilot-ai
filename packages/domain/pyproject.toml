[project]
name = "cp-domain"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["sqlalchemy>=2.0", "cp-shared"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["cp_domain*"]
